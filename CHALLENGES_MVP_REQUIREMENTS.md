# Challenges MVP — вимоги та трекер реалізації

Статус документа: `approved for implementation`

Призначення документа: зафіксувати MVP scope режиму Challenges і використовувати чекбокси нижче для відстеження реалізації.

## Правила ведення трекера

- `[ ]` — вимога ще не реалізована або не перевірена.
- `[x]` — вимога реалізована, протестована та прийнята.
- Вимога вважається виконаною лише після реалізації backend-правил, відповідного UI та перевірки критичних сценаріїв, якщо вони застосовні.
- Зміни scope спочатку вносяться в цей документ, після чого реалізуються в коді.
- Ідентифікатори вимог (`CH-PER-*`, `CH-REQ-*` тощо) не змінюються після початку реалізації.

## 1. Мета MVP

Challenges — режим, у якому гравець протягом визначеного ігрового періоду:

1. Обирає свій статус участі.
2. Знаходить суперника з іншої асоціації.
3. Створює або отримує заявку на матч.
4. Погоджує час і формат Bo3/Bo5.
5. Грає не більше одного матчу за період.
6. Переглядає та за потреби коригує результат матчу.

## 2. Зафіксовані продуктові рішення

- Розрахунок або оновлення рейтингу не входить у цей MVP.
- Логіка автоматичного пошуку та імпорту BGA-результатів не входить у цей MVP. Challenges лише використовує результат, отриманий наявним або окремо реалізованим importer.
- Результат зберігається в наявних сутностях `duels` та `games`. Окрема сутність Result не створюється.
- Коригування результату працює на довірі: зміна одного гравця застосовується одразу, без підтвердження другим гравцем.
- Challenge-матч є продуктовою назвою одиночної серії між двома гравцями. На рівні даних він зберігається як один `duel` і пов'язані з ним `games`, без створення командного запису в `matches`.
- Для Challenge-дуелей використовуються наявні статуси `duels`, розширені лише за необхідності.
- Один гравець може зіграти не більше одного Challenge-матчу в одному періоді.
- Гравець зі статусом `available` може додатково вказати до трьох часових вікон, коли він може і почати, і завершити матч. Ці вікна мають інформаційний характер і не обмежують створення заявки або запропоновані в ній часові опції.
- Сторінка Challenges має публічний preview-режим: періоди, секції заявок, список відкритих до матчу гравців і кнопки дій відображаються також неавторизованим користувачам та користувачам без прив'язаного BGA-профілю. Виконання mutating actions залишається доступним лише після авторизації та верифікації BGA-акаунта.
- Усі дати та час зберігаються в UTC.

## 3. Термінологія та статуси

### 3.1. Статуси періоду

| Статус | Значення |
|---|---|
| `draft` | Адміністратор готує період. Гравці його не бачать. |
| `planning_open` | Гравці можуть обирати статус участі та створювати заявки. |
| `active` | Можна створювати заявки та грати матчі. |
| `result_review` | Нові заявки недоступні. Гравці можуть переглядати та коригувати результати. |
| `archived` | Період повністю завершений. |
| `cancelled` | Період скасований адміністратором. |

### 3.2. Статуси гравця в періоді

Статус належить конкретній парі `player_id + period_id`.

| Статус | Відображення | Значення |
|---|---|---|
| `not_selected` | Не обрано | Гравець ще не зробив вибір або його попередній матч було скасовано. |
| `available` | Відкритий до матчу | Гравець відкритий до пропозицій. |
| `unavailable` | Закритий для матчу | Гравець не бере участі в цьому періоді. |
| `match_scheduled` | Матч заплановано | Гравець має підтверджений матч. |
| `played` | Матч зіграно | Матч завершився з результатом. |

### 3.3. Статуси заявки

| Статус | Значення |
|---|---|
| `pending` | Очікується дія гравця з `awaiting_player_id`. |
| `accepted` | Заявка прийнята та пов'язана з підтвердженим матчем. |
| `declined` | Заявка відхилена гравцем, від якого очікувалась відповідь. |
| `cancelled_by_sender` | Заявка відкликана її початковим автором. |
| `auto_cancelled` | Заявка автоматично закрита через інший підтверджений матч або зміну доступності. |
| `expired` | Минув останній запропонований у заявці час. |

### 3.4. Статуси Challenge-матчу (`duel`)

Наявні статуси `duels` позначені окремо від статусів, які треба додати для Challenges:

| Статус | Наявність | Значення |
|---|---|---|
| `Draft` | Новий | Матч підготовлений адміністратором, але ще не відкритий або не підтверджений. |
| `Requested new time` | Новий | Матч очікує погодження нового часу після запиту на перенесення. |
| `Planned` | Наявний | Матч підтверджений обома гравцями. |
| `In progress` | Новий | Час матчу настав, але завершений результат ще не отримано. |
| `Done` | Наявний | Матч зіграний; у Challenges UI відображається як `Played`. |
| `Error` | Наявний | Матч завершився, але коректний результат не отримано. |
| `Cancelled` | Новий | Матч скасований гравцем або адміністратором. |

## 4. Періоди Challenges

### 4.1. Дані періоду

- [X] **CH-PER-001** Адміністратор може створити період Challenges.
- [x] **CH-PER-002** Період містить назву та, за потреби, короткий опис.
- [X] **CH-PER-003** Період містить `planning_starts_at`.
- [x] **CH-PER-004** Період містить `play_starts_at`.
- [X] **CH-PER-005** Період містить `play_ends_at`.
- [X] **CH-PER-006** Період містить `result_review_ends_at`.
- [X] **CH-PER-007** Усі дати періоду зберігаються в UTC.
- [ ] **CH-PER-008** Backend перевіряє порядок дат: `planning_starts_at <= play_starts_at < play_ends_at <= result_review_ends_at`.
- [x] **CH-PER-009** Адміністратор може редагувати дані та дати періоду.

### 4.2. Життєвий цикл періоду

- [ ] **CH-PER-010** Новий період створюється у статусі `draft`.
- [ ] **CH-PER-011** Період у `draft` відображається гравцям без можливості змінювати статус участі або створювати заявки.
- [ ] **CH-PER-012** У `planning_open` гравці можуть змінювати статус участі та створювати заявки.
- [ ] **CH-PER-013** У `active` гравці можуть змінювати статус участі, створювати заявки та грати матчі.
- [ ] **CH-PER-014** У `result_review` не можна створювати або приймати нові заявки; усі pending-заявки періоду автоматично переходять у `expired`.
- [ ] **CH-PER-015** У `result_review` можна переглядати та коригувати результати матчів.
- [ ] **CH-PER-016** В `archived` усі player actions заблоковані.
- [ ] **CH-PER-017** Адміністратор може перевести період у `cancelled` лише після скасування незавершених матчів; при переведенні періоду у `archived` або `cancelled` усі pending-заявки цього періоду автоматично переходять у `expired`.
- [ ] **CH-PER-018** На сторінці Challenges відображаються всі періоди зі статусами `draft`, `planning_open`, `active` та `result_review`.
- [ ] **CH-PER-019** Одночасне існування поточного `active` періоду та наступного `planning_open` періоду підтримується UI та API.

## 5. Банер на головній сторінці

- [ ] **CH-BAN-001** Для відкритого planning-періоду на головній сторінці показується банер Challenges.
- [ ] **CH-BAN-002** Банер містить назву та основні дати періоду.
- [ ] **CH-BAN-003** Банер містить посилання на сторінку Challenges.
- [ ] **CH-BAN-004** Банер не показує `draft`, `archived` або `cancelled` періоди.

## 6. Статус гравця в періоді

- [x] **CH-PLY-001** Для кожного гравця та періоду існує не більше одного запису статусу.
- [x] **CH-PLY-002** Початковий статус гравця — `not_selected`.
- [x] **CH-PLY-003** Гравець може перейти з `not_selected` або `unavailable` в `available` під час `planning_open` або `active`.
- [x] **CH-PLY-004** Гравець може перейти в `unavailable`, якщо він не має `Planned` матчу в цьому періоді.
- [x] **CH-PLY-005** Перед переходом у `unavailable` UI показує попередження про автоматичне закриття заявок.
- [x] **CH-PLY-006** При переході в `unavailable` усі pending-заявки за участю цього гравця переходять у `auto_cancelled`.
- [x] **CH-PLY-008** При підтвердженні матчу статус обох гравців стає `match_scheduled`.
- [ ] **CH-PLY-009** Після отримання коректного результату статус обох гравців стає `played`.
- [ ] **CH-PLY-010** Після звичайного скасування запланованого матчу статус обох гравців стає `not_selected`.
- [ ] **CH-PLY-011** При переведенні матчу в `Requested new time` обидва гравці стають `available`.
- [x] **CH-PLY-012** Гравець із `match_scheduled` не може перейти в `unavailable`, доки не скасує матч.

### 6.1. Часові вікна доступності гравця

- [x] **CH-AVL-001** Гравець зі статусом `available` може зберегти від нуля до трьох часових вікон доступності для конкретного періоду.
- [x] **CH-AVL-002** Кожне вікно означає повний проміжок, у межах якого гравець може як почати, так і завершити матч.
- [x] **CH-AVL-003** Вікна можна змінювати лише для періоду у статусі `planning_open` або `active` і лише доки статус гравця залишається `available`.
- [x] **CH-AVL-004** Порожній масив доступності видаляє всі збережені часові вікна.
- [x] **CH-AVL-005** Початок і завершення кожного вікна мають бути валідними timestamps, `start < end` і повністю знаходитись у межах `play_starts_at..play_ends_at`.
- [x] **CH-AVL-006** Початок, завершення та тривалість кожного вікна вирівнюються до цілої години у часовій зоні асоціації гравця.
- [x] **CH-AVL-007** Часові вікна одного гравця не можуть перетинатися; backend сортує їх за часом початку перед збереженням.
- [x] **CH-AVL-008** Вікна зберігаються в UTC і повертаються API як масив об'єктів `{ start_utc, end_utc }`.
- [x] **CH-AVL-009** Player UI показує календар у часовій зоні асоціації гравця, блокує години поза ігровим вікном періоду та дозволяє створювати виділення перетягуванням.
- [x] **CH-AVL-010** Збережене вікно можна обрати, змінити перетягуванням верхньої або нижньої межі та видалити кнопкою `×` або клавішею Delete/Backspace.
- [x] **CH-AVL-011** На мобільному календар має горизонтальний скрол днів, компактні колонки та кнопки переходу до попередніх/наступних днів.
- [x] **CH-AVL-012** Список «Відкриті до матчу» показує збережені вікна суперника у часовій зоні поточного гравця.
- [x] **CH-AVL-013** Оновлення виконується через `PATCH /challenge-periods/:id/player-availability`, повторно перевіряє статус періоду та гравця і записує audit event `challenge_period_player.availability_updated`.

## 7. Доступні суперники та eligibility

- [x] **CH-ELG-001** Mutating actions Challenges виконуються лише авторизованим користувачем, пов'язаним із верифікованим профілем гравця; самі секції та кнопки можуть бути видимими у публічному preview-режимі.
- [ ] **CH-ELG-002** Active та inactive профілі можуть бути учасниками Challenges.
- [ ] **CH-ELG-003** Список «Відкриті до матчу» містить гравців зі статусом `available` у вибраному періоді.
- [ ] **CH-ELG-004** Поточний гравець не відображається як доступний суперник самому собі.
- [ ] **CH-ELG-005** У списку доступних суперників не показуються гравці з тієї самої асоціації.
- [ ] **CH-ELG-006** У списку доступних суперників не показуються гравці, які вже мають запланований матч у періоді.
- [ ] **CH-ELG-007** Через ручний вибір або сторінку Players можна запросити гравця з `not_selected`, якщо він не має інших обмежень.
- [ ] **CH-ELG-008** Не можна запросити гравця зі статусом `unavailable`.
- [ ] **CH-ELG-009** Не можна запросити гравця, який уже має запланований матч у періоді.
- [ ] **CH-ELG-010** Не можна створити другу pending-заявку між тією самою парою гравців у тому самому періоді.
- [ ] **CH-ELG-011** Після terminal-статусу попередньої заявки ту саму пару гравців можна запросити повторно.

Terminal-статуси для повторного запрошення:

- `declined`;
- `cancelled_by_sender`;
- `auto_cancelled`;
- `expired`.

### 7.1. Публічний preview та onboarding для доступу до Challenges

- [x] **CH-ONB-001** Неавторизований користувач і авторизований користувач без верифікованого BGA-профілю бачать усі відкриті Challenge-періоди, `Your period status`, секції `Requests` / `Incoming` / `Sent`, кнопку `Create request`, секцію `Open to match` та кнопки `Invite to match`; замість `Unauthorized` або `Linked player profile is required` сторінка показує звичайний UI.
- [x] **CH-ONB-002** Для користувача без повного доступу спроба змінити `Your period status`, натиснути `Create request` або `Invite to match` не виконує mutating request, а відкриває onboarding-попап у стилі Challenges.
- [x] **CH-ONB-003** Попап англійською пояснює, що для планування Challenge-матчу потрібно увійти на carcassonne.gg та верифікувати BGA-акаунт.
- [x] **CH-ONB-004** Попап містить два послідовні пункти: `Sign in to the site` і `Verify your BGA account`; виконані пункти мають completed-стан.
- [x] **CH-ONB-005** `Sign in with Google` відкриває авторизацію в окремому popup-вікні. Після успішного входу onboarding-попап залишається відкритим, повторно отримує `/auth/me` і дозволяє продовжити з другого пункту.
- [x] **CH-ONB-006** Пункт `Verify your BGA account` вважається виконаним, коли `/auth/me` повертає пов'язаний player/BGA profile ID і `Login Email` профілю.
- [x] **CH-ONB-007** До верифікації другий пункт одразу показує інструкцію без проміжного посилання `Details`: login email користувача з кнопкою копіювання, посилання на список капітанів, кроки для капітана, клікабельну назву `Players`, що веде на `https://carcassonne.gg/player-hub/players/`, і контактний email підтримки.
- [x] **CH-ONB-008** Кнопка `Check verification` повторно отримує стан користувача, під час перевірки показує `https://carcassonne.gg/gallery/loading-red.gif` і блокується від повторного натискання.
- [x] **CH-ONB-009** Якщо після ручної перевірки BGA-профіль ще не верифіковано, попап показує повідомлення `Your BGA account is not linked yet. Please contact your association captain.` і залишається відкритим.
- [x] **CH-ONB-010** Поки попап відкритий, стан BGA-верифікації додатково оновлюється автоматично; після успішної верифікації другий пункт переходить у completed-стан.
- [x] **CH-ONB-011** Коли обидва пункти виконані, footer попапа містить лише secondary-кнопку `Start playing`; вона тільки закриває попап і не повторює автоматично дію, яка відкрила onboarding.
- [x] **CH-ONB-012** `GET /challenge-periods/player` і `GET /challenge-periods/:id/eligible-opponents` підтримують публічне читання, тоді як створення заявок, зміна статусу та інші mutating Challenge endpoints залишаються захищеними авторизацією й backend eligibility-перевірками.

## 8. Створення заявки

### 8.1. Точки входу

- [ ] **CH-REQ-001** Заявку можна створити через чисту форму Player Hub → Challenges.
- [x] **CH-REQ-002** Заявку можна створити зі списку доступних гравців із попередньо обраним суперником.
- [ ] **CH-REQ-003** Заявку можна створити зі сторінки Players через кнопку «Запросити на матч» із попередньо обраним суперником.

### 8.2. Дані заявки

- [x] **CH-REQ-004** Заявка належить одному `period_id`.
- [x] **CH-REQ-005** Заявка містить двох різних учасників.
- [x] **CH-REQ-006** `created_by_player_id` вказує автора заявки.
- [x] **CH-REQ-007** `awaiting_player_id` вказує гравця, від якого зараз очікується дія.
- [x] **CH-REQ-008** Заявка містить від одного до трьох унікальних варіантів часу.
- [x] **CH-REQ-009** Кожен варіант часу знаходиться в межах `play_starts_at..play_ends_at` відповідного періоду.
- [x] **CH-REQ-010** Заявка пропонує Bo3, Bo5 або обидва формати.
- [x] **CH-REQ-011** Нова заявка створюється у статусі `pending`.
- [x] **CH-REQ-012** Заявка зберігає timestamps створення та останнього оновлення.

### 8.3. Ліміт заявок

- [x] **CH-REQ-013** Один гравець може бути автором не більше трьох pending-заявок у межах одного періоду.
- [x] **CH-REQ-014** Ліміт рахується за парою `period_id + created_by_player_id`.
- [x] **CH-REQ-015** Контрпропозиція іншого гравця не звільняє заявку з ліміту її початкового автора.
- [x] **CH-REQ-016** Перевірка ліміту виконується на backend у тій самій транзакції, що і створення заявки.

### 8.4. Відкликання та видалення

- [ ] **CH-REQ-017** Початковий автор може відкликати свою pending-заявку.
- [ ] **CH-REQ-018** Після відкликання заявка переходить у `cancelled_by_sender`.
- [ ] **CH-REQ-019** Після відкликання автор може приховати/видалити заявку зі свого списку без видалення audit history.
- [ ] **CH-REQ-020** Інший учасник не може відкликати заявку від імені її початкового автора.

### 8.5. Expiration

- [ ] **CH-REQ-021** Заявка не expire після проходження окремої часової опції, якщо в ній залишилися майбутні опції.
- [ ] **CH-REQ-022** Pending-заявка переходить у `expired` після останнього запропонованого часу.
- [ ] **CH-REQ-023** Expiration виконується автоматично та ідемпотентно.

## 9. Перегляд заявок

- [ ] **CH-LST-001** Гравець бачить список pending-заявок, у яких очікується його відповідь.
- [ ] **CH-LST-002** Гравець бачить список створених ним заявок із поточними статусами.
- [ ] **CH-LST-003** Для pending-заявки показуються запропоновані часи, формати та гравець, від якого очікується відповідь.
- [ ] **CH-LST-004** Для terminal-заявки показується її фінальний статус.
- [ ] **CH-LST-005** Гравець бачить свій підтверджений матч для кожного відкритого періоду.

## 10. Відповідь та контрпропозиція

- [ ] **CH-RSP-001** Лише гравець із `awaiting_player_id` може прийняти або відхилити pending-заявку.
- [ ] **CH-RSP-002** При відхиленні заявка переходить у `declined`.
- [ ] **CH-RSP-003** Гравець із `awaiting_player_id` може запропонувати від одного до трьох інших варіантів часу.
- [ ] **CH-RSP-005** При контрпропозиції `awaiting_player_id` змінюється на іншого учасника.
- [ ] **CH-RSP-006** При контрпропозиції статус залишається `pending`.
- [ ] **CH-RSP-007** Нові часові опції контрпропозиції також мають бути в межах ігрового періоду.
- [ ] **CH-RSP-008** Контрпропозиція записується в audit log разом з actor і змінами часових опцій.

## 11. Прийняття заявки

- [ ] **CH-ACC-001** При прийнятті гравець обирає один із поточних запропонованих часів.
- [ ] **CH-ACC-002** Якщо запропоновано обидва формати, гравець обирає Bo3 або Bo5.
- [ ] **CH-ACC-003** Якщо запропоновано один формат, він використовується автоматично.
- [ ] **CH-ACC-004** Прийняття заявки виконується однією DB-транзакцією.
- [ ] **CH-ACC-005** У транзакції повторно перевіряється, що заявка все ще `pending`.
- [ ] **CH-ACC-006** У транзакції перевіряється, що actor дорівнює `awaiting_player_id`.
- [ ] **CH-ACC-007** У транзакції перевіряється, що період дозволяє прийняття заявки.
- [ ] **CH-ACC-008** У транзакції перевіряється, що обидва гравці ще не мають іншого підтвердженого матчу в цьому періоді.
- [ ] **CH-ACC-009** Заявка переходить у `accepted`.
- [ ] **CH-ACC-010** Створюється новий `Planned` duel або підтверджується пов'язаний duel у статусі `Requested new time`.
- [ ] **CH-ACC-011** Duel містить двох гравців, погоджений час і формат.
- [ ] **CH-ACC-012** Статус обох гравців стає `match_scheduled`.
- [ ] **CH-ACC-013** Усі інші pending-заявки за участю будь-кого з цих двох гравців переходять у `auto_cancelled`.
- [ ] **CH-ACC-014** Пов'язані з auto-cancelled заявками duels у статусі `Requested new time` переходять у `Cancelled`.
- [ ] **CH-ACC-015** DB-level constraint не дозволяє одному гравцю мати більше одного активного Challenge-duel в одному періоді.
- [ ] **CH-ACC-016** Повторний однаковий accept не створює дубль duel.

### 11.1. Створення матчу «по факту»

Цей flow використовується, якщо гравці вже зіграли матч, але не створили заявку заздалегідь, або створили її, проте не встигли підтвердити до початку матчу. Отримання та імпорт фактичних результатів виконується наявним окремим функціоналом і не є частиною цього flow.

- [ ] **CH-RET-001** Під час статусу періоду `active` гравець може створити заявку з часом матчу в минулому, якщо цей час знаходиться в межах `play_starts_at..play_ends_at` відповідного періоду.
- [ ] **CH-RET-002** Заявка з часом у минулому явно позначається в UI як заявка на вже зіграний матч.
- [ ] **CH-RET-003** Інший гравець може прийняти заявку на вже зіграний матч, якщо обидва гравці ще не мають іншого підтвердженого Challenge-матчу в цьому періоді.
- [ ] **CH-RET-004** Якщо заявка була створена до матчу, але не прийнята вчасно, гравець з `awaiting_player_id` може підтвердити її після запропонованого часу як вже зіграний матч, навіть якщо заявка перейшла у статус `expired`.
- [ ] **CH-RET-005** Створення та підтвердження заявки «по факту» доступне до `play_ends_at`, а також під час `result_review`; у `archived` та `cancelled` створювати або підтверджувати такий матч не можна.
- [ ] **CH-RET-006** При підтвердженні «по факту» заявка переходить у `accepted`, а Challenge-duel створюється однією DB-транзакцією з погодженими учасниками, форматом і фактичним часом матчу в минулому.
- [ ] **CH-RET-007** До підтвердження backend повторно перевіряє статус періоду, межі фактичного часу матчу, actor, eligibility обох гравців і обмеження «не більше одного Challenge-матчу за період».
- [ ] **CH-RET-008** Після створення Challenge-duel запускається або застосовується наявний механізм отримання фактичних результатів; окрема логіка пошуку чи імпорту результатів у межах цього flow не реалізується.
- [ ] **CH-RET-009** Якщо наявний механізм одразу знаходить коректний результат, duel переходить у `Done`, а обидва гравці — у `played`; якщо результат ще не отримано, подальший статус визначається загальними правилами Challenge-duel та отримання результатів.
- [ ] **CH-RET-010** Підтвердження «по факту» автоматично закриває інші pending-заявки обох гравців за загальними правилами прийняття заявки.
- [ ] **CH-RET-011** Повторне підтвердження тієї самої заявки «по факту» не створює дубль duel і не запускає дубльоване отримання результатів.

## 12. Challenge-матч як duel

- [ ] **CH-MAT-001** Challenge-матч використовує наявну сутність `duels` і не створює запис у командній сутності `matches`.
- [ ] **CH-MAT-002** Duel пов'язаний із `challenge_period_id`.
- [ ] **CH-MAT-003** Duel пов'язаний із `challenge_request_id`.
- [ ] **CH-MAT-004** Duel має ознаку джерела `source_type = challenge` або еквівалентну однозначну ознаку.
- [ ] **CH-MAT-005** Challenge-duel не потребує батьківського командного `match`; його `match_id` залишається порожнім.
- [ ] **CH-MAT-006** Bo3 використовує відповідний наявний duel format.
- [ ] **CH-MAT-007** Bo5 використовує відповідний наявний duel format.
- [ ] **CH-MAT-008** Учасники зберігаються безпосередньо в `duels.player_1_id` і `duels.player_2_id`.
- [ ] **CH-MAT-009** Ігри Challenge-матчу зберігаються в `games` із посиланням на Challenge-duel через `games.duel_id`.
- [ ] **CH-MAT-010** На сторінці Challenges підтверджений матч показує суперника, асоціації, UTC/local time, формат і статус.

## 13. Перенесення матчу

- [ ] **CH-RSC-001** Будь-який учасник може ініціювати зміну часу як до, так і після запланованого часу початку матчу.
- [ ] **CH-RSC-003** При перенесенні пов'язана заявка повертається в `pending`.
- [ ] **CH-RSC-005** `awaiting_player_id` стає іншим учасником матчу.
- [ ] **CH-RSC-006** Матч переходить у `Requested new time`.
- [ ] **CH-RSC-007** Попередній підтверджений час не зберігається як активний слот матчу.
- [ ] **CH-RSC-008** Обидва гравці переходять у `available` і можуть отримувати або приймати інші заявки.
- [ ] **CH-RSC-009** При прийнятті нового часу заявка переходить у `accepted`, а матч — у `Planned`.
- [ ] **CH-RSC-010** Після повторного прийняття обидва гравці переходять у `match_scheduled`.
- [ ] **CH-RSC-011** При відхиленні нового часу заявка переходить у `declined`.
- [ ] **CH-RSC-012** При відхиленні нового часу пов'язаний матч у статусі `Requested new time` переходить у `Cancelled`.
- [ ] **CH-RSC-013** Після відхилення обидва гравці залишаються `available`.
- [ ] **CH-RSC-014** Якщо один з учасників прийняв іншу заявку, заявка на перенесення переходить у `auto_cancelled`, а її матч у статусі `Requested new time` переходить у `Cancelled`.

## 14. Скасування матчу

- [ ] **CH-CAN-001** Будь-який учасник може скасувати `Planned` матч як до, так і після запланованого часу його початку, якщо матч не був зіграний і результат не зафіксовано.
- [ ] **CH-CAN-002** Після запланованого часу початку скасування незіграного `Planned` матчу залишається доступним через player flow.
- [ ] **CH-CAN-003** При скасуванні матч переходить у `Cancelled` або еквівалентно soft-deleted із зафіксованою причиною скасування.
- [ ] **CH-CAN-004** При скасуванні статус обох гравців стає `not_selected`.
- [ ] **CH-CAN-005** Після скасування гравці можуть повторно перейти в `available`.
- [ ] **CH-CAN-006** Інший учасник отримує сповіщення про скасування.
- [ ] **CH-CAN-007** Адміністратор може скасувати матч незалежно від часу початку.

## 15. Результати матчу

- [ ] **CH-RES-001** Автоматично отриманий коректний результат зберігається в наявних `duels` і `games` без створення або оновлення командного `match`.
- [ ] **CH-RES-002** Після коректного завершення Challenge-duel переходить у `Done`.
- [ ] **CH-RES-003** У Challenges UI статус `Done` відображається як `Played`.
- [ ] **CH-RES-004** Після коректного завершення статус обох гравців стає `played`.
- [ ] **CH-RES-005** Якщо після завершення матчу коректний результат не отримано, Challenge-duel переходить у `Error`.
- [ ] **CH-RES-006** До `result_review_ends_at` будь-який учасник матчу може коригувати результат без підтвердження другим учасником.
- [ ] **CH-RES-007** Гравець може змінити фактичний час матчу.
- [ ] **CH-RES-008** Гравець може видалити помилково додані ігри.
- [ ] **CH-RES-009** Гравець може додати BGA table ID вручну.
- [ ] **CH-RES-010** Гравець може відхилити timeout/no-show.
- [ ] **CH-RES-011** Після коригування результат і статус Challenge-duel перераховуються на основі наявної duel/game логіки.
- [ ] **CH-RES-012** Інший учасник отримує сповіщення про зміну результату.
- [ ] **CH-RES-013** Зміни результату відображаються обом учасникам на сайті.
- [ ] **CH-RES-014** Кожна ручна зміна результату записується в audit log.
- [ ] **CH-RES-015** Після `result_review_ends_at` результат може змінювати лише адміністратор.

## 16. Сповіщення

- [ ] **CH-NTF-001** Гравець отримує сповіщення про нову заявку.
- [ ] **CH-NTF-002** Гравець отримує сповіщення про контрпропозицію.
- [ ] **CH-NTF-003** Початковий автор отримує email про прийняття заявки.
- [ ] **CH-NTF-004** Початковий автор отримує email про відхилення заявки.
- [ ] **CH-NTF-005** Гравець отримує сповіщення, якщо його заявка була автоматично закрита.
- [ ] **CH-NTF-007** Гравець отримує сповіщення про запропоноване перенесення.
- [ ] **CH-NTF-008** Гравець отримує сповіщення про скасування матчу.
- [ ] **CH-NTF-009** Гравець отримує сповіщення про ручну зміну результату іншим учасником.
- [ ] **CH-NTF-010** Повторне виконання однієї операції не створює дублікати сповіщень.

## 17. Адміністрування

- [ ] **CH-ADM-001** Адміністратор бачить список періодів Challenges.
- [ ] **CH-ADM-002** Адміністратор може створювати та редагувати періоди.
- [ ] **CH-ADM-003** Адміністратор може змінювати статус періоду відповідно до дозволених переходів.
- [x] **CH-ADM-004** Адміністратор бачить усі заявки періоду, включно з terminal-статусами.
- [ ] **CH-ADM-005** Адміністратор бачить статус кожного гравця у вибраному періоді.
- [ ] **CH-ADM-006** Адміністратор може вручну створити Challenge-матч.
- [ ] **CH-ADM-007** Адміністратор може редагувати Challenge-матч.
- [ ] **CH-ADM-008** Адміністратор може переносити Challenge-матч без player approval flow.
- [ ] **CH-ADM-009** Адміністратор може скасувати Challenge-матч.
- [ ] **CH-ADM-010** Адміністратор може коригувати результат після `result_review_ends_at`.
- [x] **CH-ADM-011** Над усіма блоками сторінки Challenges global admin бачить перемикач `Admin mode`; для інших користувачів цей перемикач не відображається.
- [x] **CH-ADM-012** Увімкнений `Admin mode` показує в кожному відкритому Challenge-періоді всі заявки всіх гравців, а не лише заявки за участю поточного користувача.
- [x] **CH-ADM-013** Увімкнений `Admin mode` показує всі Challenge-матчі періоду, включно з duel, створеним адміністратором без `challenge_request_id`.
- [x] **CH-ADM-014** Після ввімкнення `Admin mode` поруч з'являється перемикач `Removed items`; після його ввімкнення до списків додаються приховані заявки з `hidden_by_creator_at IS NOT NULL` і soft-deleted duels із `deleted_at IS NOT NULL`.
- [x] **CH-ADM-015** Заявки та матчі інших гравців у `Admin mode` відображаються read-only без player actions для прийняття, відхилення, перенесення, скасування або видалення.
- [x] **CH-ADM-016** Backend дозволяє параметри `admin_mode=1` та `include_removed=1` лише global admin; запит цих режимів авторизованим не-адміністратором повертає `403` і не розкриває чужі або видалені записи.
- [x] **CH-ADM-017** Поза `Admin mode` API та UI зберігають player-scoped фільтрацію, а приховані заявки й soft-deleted duels не відображаються.

## 18. Audit log

- [ ] **CH-AUD-001** Створення заявки записується в audit log.
- [ ] **CH-AUD-002** Контрпропозиція записується в audit log.
- [ ] **CH-AUD-003** Прийняття заявки записується в audit log.
- [ ] **CH-AUD-004** Відхилення заявки записується в audit log.
- [ ] **CH-AUD-005** Відкликання заявки записується в audit log.
- [ ] **CH-AUD-006** Автоматичне закриття заявки записується в audit log.
- [ ] **CH-AUD-007** Expiration заявки записується в audit log.
- [ ] **CH-AUD-008** Для кожної події зберігаються actor, action, request ID, previous state, next state та timestamp.
- [ ] **CH-AUD-009** Історія audit log не видаляється при приховуванні заявки гравцем.

## 19. Конкурентність та цілісність даних

- [ ] **CH-CON-001** Усі multi-entity переходи статусів виконуються транзакційно.
- [ ] **CH-CON-002** Два одночасні accept для одного гравця не можуть створити два підтверджені Challenge-duels.
- [ ] **CH-CON-003** Accept і перехід гравця в `unavailable`, виконані одночасно, завершуються одним валідним станом без частково оновлених даних.
- [ ] **CH-CON-004** Accept заявки, яка вже стала terminal, повертає conflict і не змінює дані.
- [ ] **CH-CON-005** Повторна доставка одного API-запиту не створює дублікати duels, games, audit events або сповіщень.
- [ ] **CH-CON-006** Soft-deleted/Cancelled матч не блокує створення нового матчу для гравців у тому самому періоді.
- [ ] **CH-CON-007** Матч у статусі `Requested new time` не вважається запланованим і не блокує інші заявки.

## 20. Критичні acceptance-сценарії

- [ ] **CH-E2E-001** Гравець стає available, створює заявку, суперник приймає її, матч створюється, обидва отримують `match_scheduled`.
- [ ] **CH-E2E-002** Гравець із трьома створеними pending-заявками не може створити четверту.
- [ ] **CH-E2E-003** Контрпропозиція змінює `awaiting_player_id` і не впливає на ліміт початкового автора.
- [ ] **CH-E2E-004** Після decline або cancel ту саму пару гравців можна запросити повторно з іншим часом.
- [ ] **CH-E2E-005** Accept однієї заявки автоматично закриває всі інші pending-заявки обох гравців.
- [ ] **CH-E2E-006** Два одночасні accept для одного гравця завершуються створенням лише одного підтвердженого матчу.
- [ ] **CH-E2E-007** Перехід в unavailable закриває відповідні заявки та не дозволяється при запланованому матчі.
- [ ] **CH-E2E-008** Перенесення переводить матч у `Requested new time`, заявку в `pending`, а гравців у `available`.
- [ ] **CH-E2E-009** Відхилення перенесення переводить матч із `Requested new time` у `Cancelled` і залишає обох гравців `available`.
- [x] **CH-E2E-010** Поки час матчу перепогоджується і матч перебуває у `Requested new time`, гравець може підтвердити матч з іншим гравцем; після цього заявка на перенесення автоматично переходить у `auto_cancelled`, а попередній матч переходить у `Cancelled`.
- [ ] **CH-E2E-011** Незіграний `Planned` матч без зафіксованого результату можна скасувати через player flow як до, так і після запланованого часу старту.
- [ ] **CH-E2E-012** Ручна зміна результату одразу видима обом гравцям і створює сповіщення та audit event.
- [ ] **CH-E2E-013** Після `result_review_ends_at` гравець не може змінити результат, а адміністратор може.
- [ ] **CH-E2E-014** Запропонований час поза межами ігрового періоду відхиляється backend.
- [ ] **CH-E2E-015** Заявка expire лише після проходження останньої запропонованої часової опції.
- [ ] **CH-E2E-016** Під час `active` гравець створює заявку з часом уже зіграного матчу в межах періоду, суперник приймає її, duel створюється та отримує результати через наявний функціонал.
- [ ] **CH-E2E-017** Заявку, яку не прийняли до запропонованого часу, можна підтвердити «по факту» до `play_ends_at`; при цьому створюється лише один duel, а результат отримується через наявний функціонал.
- [x] **CH-E2E-018** Гравець у статусі `available` створює до трьох непересічних погодинних вікон у локальному календарі, зберігає їх у UTC, редагує або видаляє, а інший гравець бачить ці вікна у своїй часовій зоні.
- [ ] **CH-E2E-019** Неавторизований користувач бачить повний preview сторінки Challenges; кожна з трьох gated actions відкриває onboarding, Google sign-in не закриває основний попап, ручна BGA-перевірка показує loading/error state, а після виконання обох пунктів `Start playing` закриває попап без автоматичного повторення початкової дії.
- [ ] **CH-E2E-020** Global admin вмикає `Admin mode`, бачить чужі заявки та матчі без player actions, потім вмикає `Removed items` і додатково бачить приховані заявки та soft-deleted duels; не-адміністратор не бачить перемикачів і отримує `403` при прямому запиті admin-параметрів API.

## 21. Схема даних нових DB-об'єктів

Схема нижче є цільовою для SQLite. Усі ідентифікатори нових сутностей мають тип `TEXT`, усі timestamps зберігаються як UTC ISO 8601 у полях типу `TEXT`, а boolean-значення — як `INTEGER` зі значенням `0` або `1`.

### 21.1. `challenge_periods`

Один запис описує один період Challenges.

| Поле | Тип | Null | Default | Призначення |
|---|---|---:|---|---|
| `id` | `TEXT` | ні | — | Primary key. |
| `name` | `TEXT` | ні | — | Назва періоду. |
| `description` | `TEXT` | так | `NULL` | Короткий опис. |
| `logo` | `TEXT` | так | `NULL` | Посилання на картинку логотипа періоду. |
| `status` | `TEXT` | ні | `'draft'` | `draft`, `planning_open`, `active`, `result_review`, `archived` або `cancelled`. |
| `planning_starts_at` | `TEXT` | ні | — | Початок планування в UTC. |
| `play_starts_at` | `TEXT` | ні | — | Початок ігрового періоду в UTC. |
| `play_ends_at` | `TEXT` | ні | — | Завершення ігрового періоду в UTC. |
| `result_review_ends_at` | `TEXT` | ні | — | Завершення перевірки результатів у UTC. |
| `created_by` | `TEXT` | так | `NULL` | Ідентифікатор адміністратора, який створив запис. |
| `updated_by` | `TEXT` | так | `NULL` | Ідентифікатор адміністратора, який востаннє змінив запис. |
| `created_at` | `TEXT` | ні | `CURRENT_TIMESTAMP` | Час створення. |
| `updated_at` | `TEXT` | ні | `CURRENT_TIMESTAMP` | Час останньої зміни. |

Обов'язкові constraints та індекси:

- `CHECK (planning_starts_at <= play_starts_at AND play_starts_at < play_ends_at AND play_ends_at <= result_review_ends_at)`;
- `CHECK` для дозволених значень `status`;
- індекс `(status, planning_starts_at, play_ends_at, result_review_ends_at)` для пошуку відкритих періодів.

### 21.2. `challenge_period_players`

Один запис зберігає стан одного гравця в одному періоді.

| Поле | Тип | Null | Default | Призначення |
|---|---|---:|---|---|
| `period_id` | `TEXT` | ні | — | FK → `challenge_periods.id`. |
| `player_id` | `TEXT` | ні | — | FK → `profiles.id`. |
| `status` | `TEXT` | ні | `'not_selected'` | `not_selected`, `available`, `unavailable`, `match_scheduled` або `played`. |
| `challenge_duel_id` | `TEXT` | так | `NULL` | FK → `duels.id`; поточний підтверджений або зіграний Challenge-duel гравця. Для `Requested new time` і після скасування — `NULL`. |
| `availability_start_1_utc` | `TEXT` | так | `NULL` | Початок першого вікна доступності в UTC. |
| `availability_end_1_utc` | `TEXT` | так | `NULL` | Завершення першого вікна доступності в UTC. |
| `availability_start_2_utc` | `TEXT` | так | `NULL` | Початок другого вікна доступності в UTC. |
| `availability_end_2_utc` | `TEXT` | так | `NULL` | Завершення другого вікна доступності в UTC. |
| `availability_start_3_utc` | `TEXT` | так | `NULL` | Початок третього вікна доступності в UTC. |
| `availability_end_3_utc` | `TEXT` | так | `NULL` | Завершення третього вікна доступності в UTC. |
| `created_at` | `TEXT` | ні | `CURRENT_TIMESTAMP` | Час створення. |
| `status_updated_at` | `TEXT` | ні | `CURRENT_TIMESTAMP` | Час останньої фактичної зміни `status`; використовується для сортування «Відкриті до матчу». |
| `updated_at` | `TEXT` | ні | `CURRENT_TIMESTAMP` | Час останньої зміни. |

Обов'язкові constraints та індекси:

- `PRIMARY KEY (period_id, player_id)`;
- `CHECK` для дозволених значень `status`;
- `CHECK`, що для `match_scheduled` і `played` задано `challenge_duel_id`, а для інших статусів воно дорівнює `NULL`;
- кожне вікно доступності зберігається лише повною парою start/end; backend перевіряє максимум три вікна, межі періоду, цілі години, додатну тривалість і відсутність перетинів;
- індекси `(period_id, status)` і `(period_id, status, status_updated_at)` для списку та сортування доступних гравців;
- прийняття матчу атомарно встановлює `challenge_duel_id` обом гравцям лише якщо воно ще `NULL`; це поле є DB-рівнем блокування другого підтвердженого матчу в тому самому періоді.

### 21.3. `challenge_requests`

Один запис зберігає заявку або поточну контрпропозицію між двома гравцями.

| Поле | Тип | Null | Default | Призначення |
|---|---|---:|---|---|
| `id` | `TEXT` | ні | — | Primary key. |
| `period_id` | `TEXT` | ні | — | FK → `challenge_periods.id`. |
| `player_1_id` | `TEXT` | ні | — | FK → `profiles.id`; перший учасник. |
| `player_2_id` | `TEXT` | ні | — | FK → `profiles.id`; другий учасник. |
| `created_by_player_id` | `TEXT` | ні | — | FK → `profiles.id`; початковий автор, не змінюється при контрпропозиціях. |
| `awaiting_player_id` | `TEXT` | ні | — | FK → `profiles.id`; учасник, від якого очікується наступна дія. |
| `status` | `TEXT` | ні | `'pending'` | `pending`, `accepted`, `declined`, `cancelled_by_sender`, `auto_cancelled` або `expired`. |
| `time_option_1_utc` | `TEXT` | ні | — | Перший запропонований час у UTC. |
| `time_option_2_utc` | `TEXT` | так | `NULL` | Другий запропонований час у UTC. |
| `time_option_3_utc` | `TEXT` | так | `NULL` | Третій запропонований час у UTC. |
| `allows_bo3` | `INTEGER` | ні | `0` | Чи доступний формат Bo3. |
| `allows_bo5` | `INTEGER` | ні | `0` | Чи доступний формат Bo5. |
| `accepted_time_utc` | `TEXT` | так | `NULL` | Обраний або фактичний час після прийняття. |
| `accepted_format` | `TEXT` | так | `NULL` | Погоджений формат: `Bo3` або `Bo5`. |
| `hidden_by_creator_at` | `TEXT` | так | `NULL` | Приховує terminal-заявку зі списку автора без фізичного видалення. |
| `created_at` | `TEXT` | ні | `CURRENT_TIMESTAMP` | Час створення. |
| `updated_at` | `TEXT` | ні | `CURRENT_TIMESTAMP` | Час останньої зміни або контрпропозиції. |

Обов'язкові constraints та індекси:

- `CHECK (player_1_id <> player_2_id)`;
- `CHECK`, що `created_by_player_id` і `awaiting_player_id` є одним із двох учасників;
- `CHECK (allows_bo3 = 1 OR allows_bo5 = 1)` та перевірка boolean-полів на `0/1`;
- `CHECK` для дозволених статусів і форматів;
- `CHECK`, що `time_option_1_utc` заданий, `time_option_3_utc` не може бути заданий без `time_option_2_utc`, а всі задані часові опції унікальні;
- для `accepted` обидва поля `accepted_time_utc` та `accepted_format` обов'язкові;
- backend перевіряє, що `accepted_time_utc` дорівнює одній із поточних часових опцій заявки;
- при контрпропозиції `time_option_1_utc`, `time_option_2_utc` і `time_option_3_utc` замінюються транзакційно, а попереднє значення залишається в `audit_trail`;
- межі `play_starts_at..play_ends_at` для часових опцій перевіряються backend у транзакції, оскільки залежать від `challenge_periods`;
- partial unique index для однієї `pending`-заявки на нормалізовану пару `(period_id, min(player_1_id, player_2_id), max(player_1_id, player_2_id))`;
- індекси `(awaiting_player_id, status)`, `(created_by_player_id, status)` і `(period_id, status)`;
- backend рахує ліміт із трьох заявок через `(period_id, created_by_player_id, status = 'pending')` у транзакції створення.

### 21.4. `notifications`

Універсальна таблиця є outbox та сховищем in-app сповіщень для всіх майбутніх доменів сайту. Один логічний event може створити окремі записи для каналів `in_app` та `email`. Для Challenges доменний контекст зберігається через `domain`, `event_type`, `source_entity_type`, `source_entity_id` і `payload`, без окремих Challenge-specific колонок.

| Поле | Тип | Null | Default | Призначення |
|---|---|---:|---|---|
| `id` | `TEXT` | ні | — | Primary key. |
| `recipient_user_id` | `INTEGER` | ні | — | FK → `users.id`; користувач, якому адресоване сповіщення. |
| `domain` | `TEXT` | ні | — | Домен події: `challenge`, `tournament`, `news`, `system` тощо. |
| `event_type` | `TEXT` | ні | — | Namespaced тип події, наприклад `challenge.request.created` або `challenge.duel.cancelled`. |
| `source_entity_type` | `TEXT` | так | `NULL` | Тип об'єкта-джерела: `challenge_request`, `challenge_duel`, `tournament`, `news_post` тощо. |
| `source_entity_id` | `TEXT` | так | `NULL` | ID об'єкта-джерела. |
| `channel` | `TEXT` | ні | `'in_app'` | `in_app` або `email`. |
| `payload` | `TEXT` | ні | `'{}'` | JSON-дані для відображення, email template та доменного контексту. |
| `deduplication_key` | `TEXT` | ні | — | Стабільний ключ ідемпотентності події, отримувача та каналу. |
| `delivery_status` | `TEXT` | ні | `'pending'` | `pending`, `sent` або `failed`. |
| `sent_at` | `TEXT` | так | `NULL` | Час успішної доставки. |
| `read_at` | `TEXT` | так | `NULL` | Час прочитання in-app сповіщення. |
| `last_error` | `TEXT` | так | `NULL` | Остання помилка доставки. |
| `created_at` | `TEXT` | ні | `CURRENT_TIMESTAMP` | Час створення. |
| `updated_at` | `TEXT` | ні | `CURRENT_TIMESTAMP` | Час останньої зміни. |

Обов'язкові constraints та індекси:

- `UNIQUE (deduplication_key)` для захисту від дубльованих сповіщень;
- `CHECK` для `domain`, `event_type`, `channel` і `delivery_status`;
- `CHECK`, що `source_entity_type` і `source_entity_id` або обидва задані, або обидва `NULL`;
- індекси `(recipient_user_id, channel, read_at, created_at)`, `(delivery_status, channel, created_at)` та `(domain, source_entity_type, source_entity_id)`.

### 21.5. Розширення наявної таблиці `duels`

Для Challenge-матчів до `duels` додаються поля:

| Поле | Тип | Null | Default | Призначення |
|---|---|---:|---|---|
| `challenge_period_id` | `TEXT` | так | `NULL` | FK → `challenge_periods.id`; заданий лише для Challenge-duel. |
| `challenge_request_id` | `TEXT` | так | `NULL` | FK → `challenge_requests.id`; `NULL` для матчу, створеного адміністратором без заявки. |
| `source_type` | `TEXT` | так | `NULL` | Для Challenge-duel дорівнює `challenge`. |
| `cancelled_by_player_id` | `TEXT` | так | `NULL` | ID гравця, який скасував матч; `NULL`, якщо матч не скасовано. Для admin cancellation використовується sentinel-значення `'1'`. |
| `cancellation_reason` | `TEXT` | так | `NULL` | Зафіксована причина скасування. |
| `cancelled_at` | `TEXT` | так | `NULL` | Час скасування в UTC. |

Для Challenge-duel наявні поля використовуються так:

- `match_id = NULL`;
- `player_1_id` і `player_2_id` містять учасників;
- `time_utc` містить погоджений, а після коригування — фактичний час матчу;
- `duel_format` містить `Bo3` або `Bo5` відповідно до запису в `duel_formats`;
- `status` містить `Draft`, `Requested new time`, `Planned`, `In progress`, `Done`, `Error` або `Cancelled`;
- `deleted_at` не використовується для звичайного Challenge-cancel, якщо зберігається статус `Cancelled`.

Обов'язкові constraints та індекси:

- unique partial index на `challenge_request_id`, коли він не `NULL`, щоб одна заявка не створила два duels;
- індекси `(challenge_period_id, status)` та `(source_type, player_1_id, player_2_id)`;
- `CHECK`, що при `source_type = 'challenge'` задані `challenge_period_id`, обидва різні гравці, `match_id IS NULL` і дозволений Challenge-статус;
- `cancelled_by_player_id` посилається на `profiles.id` для player cancellation; значення `'1'` зарезервоване для admin cancellation;
- DB trigger або еквівалентна транзакційна перевірка узгоджує `challenge_period_players.challenge_duel_id` з учасниками duel і не дозволяє другий активний/зіграний Challenge-duel у періоді.

### 21.6. Розширення наявної таблиці `audit_trail`

Нова таблиця audit log не створюється. До наявної `audit_trail` додаються:

| Поле | Тип | Null | Default | Призначення |
|---|---|---:|---|---|
| `actor_player_id` | `TEXT` | так | `NULL` | FK → `profiles.id`; Challenge-гравець, який виконав дію. |
| `idempotency_key` | `TEXT` | так | `NULL` | Стабільний ключ для недопущення дубля audit event при повторному API-запиті. |

Для Challenge-подій `entity_type` містить `challenge_period`, `challenge_request` або `challenge_duel`, `record_id` — ID відповідного об'єкта, `changes` — JSON із previous/next state, а `metadata` — пов'язані `period_id`, `request_id`, `duel_id` та технічний контекст операції. Для `idempotency_key IS NOT NULL` створюється unique partial index.

### 21.7. Об'єкти, які не створюються

- Окрема таблиця результатів не потрібна: результат зберігається в наявних `duels` і `games`.
- Окремий Challenge-об'єкт матчу не потрібний: матч зберігається як `duels` із `source_type = 'challenge'`.
- Окрема таблиця `challenge_request_time_options` для MVP не створюється: три поточні часові опції зберігаються полями `time_option_1_utc`, `time_option_2_utc` і `time_option_3_utc` у `challenge_requests`.
- Окрема таблиця часових вікон доступності для MVP не створюється: до трьох пар start/end зберігаються безпосередньо в `challenge_period_players`.
- Окрема таблиця `challenge_notifications` не створюється: Challenge-сповіщення зберігаються в універсальній таблиці `notifications` із `domain = 'challenge'`.
- `games` не потребує нових Challenge-полів і пов'язується через наявний `games.duel_id`.
- Командна таблиця `matches` для Challenges не використовується.

## 22. Implementation tracker

Ця секція відстежує великі технічні блоки. Детальна готовність визначається чекбоксами вимог вище.

- [x] **CH-IMP-001** Схема БД та міграції.
- [x] **CH-IMP-002** Period API та admin UI.
- [ ] **CH-IMP-003** Player-period status API та UI.
- [x] **CH-IMP-004** Eligibility та список доступних суперників.
- [x] **CH-IMP-005** Request API та player UI.
- [ ] **CH-IMP-006** Accept/counteroffer/cancel/expire state transitions.
- [ ] **CH-IMP-007** Інтеграція Challenge-заявок з `duels` і `games` без використання командних `matches`.
- [ ] **CH-IMP-008** Reschedule та match cancellation flows.
- [ ] **CH-IMP-009** Result review та ручне коригування.
- [ ] **CH-IMP-010** Сповіщення та email.
- [ ] **CH-IMP-011** Банер на головній сторінці.
- [ ] **CH-IMP-012** Audit log.
- [ ] **CH-IMP-013** Background expiration/status jobs.
- [ ] **CH-IMP-014** Automated tests критичних state transitions і race conditions.
- [ ] **CH-IMP-015** End-to-end перевірка MVP.
- [x] **CH-IMP-016** Часові вікна доступності гравця: DB-поля, API, audit, desktop/mobile календар і відображення суперникам.
- [x] **CH-IMP-017** Публічний preview Challenges та onboarding для sign-in/BGA verification перед mutating actions.
- [x] **CH-IMP-018** Global-admin режим на сторінці Challenges: усі заявки й матчі, read-only відображення, опційне включення soft-deleted записів і backend access control.

## 23. Поза межами MVP

- Розрахунок та оновлення рейтингу.
- Реалізація або зміна механізму автоматичного імпорту BGA-результатів.
- Report an Issue.
- Штрафи за no-show, неправильний результат або пізнє скасування.
- Спеціальна логіка зміни асоціації посеред періоду.
- Історичний UI для архівних періодів, заявок, матчів і rating delta.
- Розширені admin-фільтри, пошук та експорт.
- Нагадування перед матчем.
- Нагадування перед завершенням result review period.

## 24. Після MVP

Наступні функції можуть використовувати збережені Challenge-матчі та audit history, але потребують окремого scope:

- вплив результатів Challenges на рейтинг;
- автоматичне зіставлення BGA tables із Challenge-матчем;
- Report an Issue та admin dispute workflow;
- санкції за зловживання;
- історія участі та статистика Challenges;
- нагадування та розширені канали сповіщень.
